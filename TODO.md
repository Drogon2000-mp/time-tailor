# TODO

## Deploy fix: Render build/publish failure
- [ ] Fix `server/render.yaml` to use the correct start command (`node server.js`) and remove frontend-style build/publish expectations.
- [ ] (After change) run a local build sanity check for backend start command.

